# EECS-1021-Plant-Watering-Project

## Default Branch

**Note:** The default branch for this project is `master`. All development work is in the `master` branch.

## Branch Management

If you see a `main` branch in this repository, it should be deleted as it only contains the initial commit. All actual project work is in the `master` branch.

For instructions on deleting the `main` branch, see [DELETE_MAIN_BRANCH_INSTRUCTIONS.md](DELETE_MAIN_BRANCH_INSTRUCTIONS.md) or run the `delete-main-branch.sh` script.